
** DEMO NOTES **

./libarch-debug f44fbea9 1
./libarch-debug ffc300d1 0

rasm2 -a arm -b 64 -d <opcode>


./htool macho -h -l htool
./htool analyse -a -l kernel.arm64
./htool analyse -a -e com.apple.driver.AppleARMPlatform kernel.arm64

% This shows tagged pointers, i haven't implemented untagging yet, so i'll use the untagged pointer that's
% written down.
./htool macho -h -l com.apple.driver.AppleARMPlatform
./htool disass -d -b 0xfffffff0082bd750 kernel.arm64

% iboot example
./htool analyse -a iboot.arm64
./htool disass -d -c 512 iboot.arm64 | less

./htool disass -d -b 0x00000000000004e4 iboot.arm64

% shows symbol recognition
./htool disass -d -c 1024 htool